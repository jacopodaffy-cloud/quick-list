/* Template for config.js — copy to config.js and fill in your Firebase WEB config.
   These values are PUBLIC by design (they ship to every browser); the real
   protection is the Firestore rules + App Check. See SECURITY.md.
   NEVER put a service-account / admin / private key in here. */
window.QUICKLIST_CONFIG = {
  firebase: null
  // firebase: {
  //   apiKey: "AIza...",
  //   authDomain: "your-app.firebaseapp.com",
  //   projectId: "your-app",
  //   storageBucket: "your-app.appspot.com",
  //   messagingSenderId: "000000000000",
  //   appId: "1:000000000000:web:abc123"
  // }
};
